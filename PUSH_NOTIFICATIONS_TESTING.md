# Инструкция по тестированию Push-уведомлений

## Подготовка к тестированию

### 1. Очистка кэша и Service Workers
Перед началом тестирования необходимо очистить все данные:

1. Открыть DevTools (F12)
2. Application → Storage → Clear site data
3. Application → Service Workers → Unregister all
4. Перезагрузить страницу

### 2. Проверка консоли
Во время тестирования следите за консолью браузера - там появятся подробные логи с эмодзи для удобства:
- 🚀 - Начало процесса
- ✅ - Успешная операция
- ❌ - Ошибка
- ⚠️ - Предупреждение
- 📨 - Получено сообщение

## Сценарии тестирования

### Сценарий 1: Первый запуск (новый пользователь)

**Шаги:**
1. Очистить все данные (см. выше)
2. Открыть приложение
3. Авторизоваться

**Ожидаемые логи в консоли:**
```
Firebase initialized successfully
🚀 Initializing notification service...
📝 Registering Service Worker...
✅ Service Worker registered successfully
⚙️ Loading notification settings from server...
📦 Server settings received: {newBooking: true, ...}
✅ Settings loaded: {enabled: false, types: {...}}
🔕 Notifications are disabled in settings...
```

**Результат:**
- Service Worker зарегистрирован ✅
- Уведомления отключены (нет разрешения браузера) ✅
- Токен НЕ отправлен на сервер ✅

### Сценарий 2: Включение уведомлений

**Шаги:**
1. Перейти в Настройки → Уведомления
2. Включить переключатель "Включить уведомления"
3. Разрешить уведомления в браузере

**Ожидаемые логи в консоли:**
```
🚀 Initializing notification service...
🔐 Current notification permission: granted
🎟️ Getting FCM token...
🔑 Starting FCM token retrieval...
📱 Notification permission: granted
✅ Service Worker already registered
🔑 Requesting FCM token with VAPID key...
✅ FCM Token received: eJYxG...
Full token for debugging: <полный_токен>
📤 Sending token to server...
Sending token to server: {token: "eJYxG...", deviceId: "web_...", ...}
✅ Token successfully sent to server
🎧 Setting up foreground message handler...
✅ Notification service initialized successfully
```

**Результат:**
- Получен FCM токен ✅
- Токен отправлен на сервер ✅
- Настроен обработчик foreground сообщений ✅

### Сценарий 3: Тестовое уведомление (локальное)

**Шаги:**
1. В настройках уведомлений нажать "Отправить тестовое уведомление"

**Ожидаемый результат:**
- Появляется браузерное уведомление "Тестовое уведомление" ✅
- В консоли: `Test notification shown` ✅
- Toast: "Тестовое уведомление показано" ✅

### Сценарий 4: Push-уведомление через Firebase Console

**Подготовка:**
1. Получить FCM токен из консоли DevTools (он выводится при включении уведомлений)
2. Открыть Firebase Console: https://console.firebase.google.com/
3. Перейти в проект `prosto-b13d3`
4. Messaging → New campaign → Firebase Notification messages → Create

**Настройка уведомления:**
- **Title:** Тест с Firebase
- **Body:** Это тестовое уведомление из Firebase Console
- **Target:** Single device
- **FCM registration token:** <вставить_токен>

**Тестирование foreground (приложение открыто):**
1. Отправить уведомление
2. Проверить консоль

**Ожидаемые логи:**
```
Foreground message received: {notification: {...}, data: {...}}
```

**Результат:**
- Появляется toast с текстом уведомления ✅

**Тестирование background (приложение свернуто/закрыто):**
1. Свернуть/закрыть вкладку с приложением
2. Отправить уведомление из Firebase Console
3. Открыть консоль Service Worker: DevTools → Application → Service Workers → firebase-messaging-sw.js

**Ожидаемые логи в Service Worker:**
```
[firebase-messaging-sw.js] 📨 Received background message: {...}
[firebase-messaging-sw.js] Notification data: {title: "...", body: "..."}
[firebase-messaging-sw.js] 🔔 Showing notification: ...
```

**Результат:**
- Появляется системное уведомление ✅
- При клике на уведомление открывается/фокусируется приложение ✅

### Сценарий 5: Push-уведомление с сервера

**Требования:**
- Токен должен быть отправлен на сервер
- На сервере должен быть реализован endpoint для отправки push-уведомлений

**Проверка:**
1. Убедиться что токен отправлен (см. Сценарий 2)
2. Попросить backend отправить тестовое уведомление через API
3. Проверить получение уведомления

**Формат данных для сервера:**
```json
{
  "token": "<fcm_token>",
  "notification": {
    "title": "Новый заказ",
    "body": "У вас новый заказ #12345"
  },
  "data": {
    "type": "booking_created",
    "booking_id": "12345"
  }
}
```

## Частые проблемы и решения

### Проблема: Service Worker не регистрируется

**Проверить:**
1. Файл `firebase-messaging-sw.js` доступен по пути `/firebase-messaging-sw.js`
2. Нет ошибок в консоли браузера
3. HTTPS или localhost (Service Worker работает только на безопасных origin)

**Решение:**
- Убедиться что файл находится в `public/` и копируется в `dist/` при сборке
- Проверить конфигурацию Firebase в SW

### Проблема: Токен не получается

**Проверить:**
1. Разрешение браузера: `Notification.permission === 'granted'`
2. VAPID ключ правильный
3. Service Worker зарегистрирован

**Логи:**
```
❌ Failed to get FCM token
⚠️ Notification permission not granted
```

**Решение:**
- Запросить разрешение заново
- Проверить VAPID ключ в `.env`

### Проблема: Уведомления не приходят

**Foreground (приложение открыто):**
1. Проверить что `setupForegroundMessageHandler()` был вызван
2. Проверить консоль на наличие логов: `Foreground message received`
3. Убедиться что Firebase отправляет `notification` payload

**Background (приложение закрыто):**
1. Проверить консоль Service Worker
2. Убедиться что SW активен: DevTools → Application → Service Workers
3. Проверить формат уведомления - должно быть поле `notification`

### Проблема: Токен отправлен, но сервер не отправляет уведомления

**Проверить на сервере:**
1. Токен сохранен в базе данных
2. Firebase Admin SDK настроен правильно
3. Сервер использует правильный project ID и credentials
4. Логи сервера на наличие ошибок от FCM

## Отладка

### Просмотр всех зарегистрированных Service Workers
```javascript
navigator.serviceWorker.getRegistrations().then(registrations => {
  console.log('Registered SWs:', registrations);
});
```

### Проверка текущего разрешения
```javascript
console.log('Permission:', Notification.permission);
```

### Ручное получение токена
```javascript
// В консоли браузера
import { notificationService } from './src/services/notificationService';
const token = await notificationService.getToken();
console.log('Token:', token);
```

### Просмотр токена в настройках (dev mode)
В development mode токен отображается в настройках уведомлений в секции "Информация для разработки"

## Контрольный чек-лист

Перед тем как сообщить о готовности:

- [ ] Service Worker регистрируется автоматически после авторизации
- [ ] При включении уведомлений получается FCM токен
- [ ] Токен успешно отправляется на сервер (логи ✅)
- [ ] Тестовое уведомление (кнопка) работает
- [ ] Foreground уведомления показываются (toast)
- [ ] Background уведомления показываются (системное уведомление)
- [ ] Клик по уведомлению открывает/фокусирует приложение
- [ ] При отключении уведомлений токен удаляется с сервера
- [ ] При повторном запуске приложения уведомления восстанавливаются

## Стейдж URL

Стейдж: https://dev.prosto-app.ru/ (или https://dev.pro-sto.ru/)

**Важно:** На стейдже должны быть те же Firebase credentials что и в `.env`
